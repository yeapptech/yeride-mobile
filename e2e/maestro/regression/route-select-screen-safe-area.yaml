appId: app.yeride.dev
# Regression: Confirm button row has no safe-area bottom inset on RouteSelectScreen
# Category: safe-area
# Fix: Add 'bottom' to the root SafeAreaView edges, or wrap the button row in its own <SafeAreaView edges={['bottom']}> to keep the map/scroll full-bleed.
# TODO: add the specific tap/scroll/assert sequence to reproduce the issue
---
- runFlow: ../_lib/sign-in-as.yaml
  env:
    EMAIL: ${RIDER_EMAIL}
    PASSWORD: ${RIDER_PASSWORD}
# TODO: navigate to RouteSelectScreen and reproduce: The sticky bottom <View className="border-t border-border px-4 py-3"> holding the Confirm/Schedule Pressable is a direct child of <SafeAreaView edges={['top']}>. The `edges` array does not include 'bottom', so the button overlaps the home indicator on notched iPhones.
