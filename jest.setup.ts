// Global Jest setup. Mocks for native-only modules (Reanimated, GestureHandler,
// Firebase, Stripe, etc.) will be added here as features land.
//
// For Phase 0 we have nothing platform-specific to mock yet; this file exists
// so the path referenced by jest.config.ts resolves.

export {};
